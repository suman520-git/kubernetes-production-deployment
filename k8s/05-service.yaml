apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: k8s-demo
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
